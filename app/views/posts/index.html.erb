<h3>All posts:</h3>
    <ul>
        <% @posts.each do |post| %>
            <li><%= link_to post.name, post_path(post) %></li>
        <% end %>
    </ul>

<%= link_to 'Create a new post', new_post_path %>
